============
Installation
============

At the command line::

    $ pip install tatu_dashboard

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tatu_dashboard
    $ pip install tatu_dashboard